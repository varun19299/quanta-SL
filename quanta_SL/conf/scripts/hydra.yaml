# Hydra configs
hydra:
  output_subdir: null # disable shifting output directories
  run:
    dir: ./outputs/scripts/${exp_name}/
  sweep:
    dir: ./outputs/scripts/${exp_name}/
  job:
    config:
      override_dirname:
        exclude_keys:
          - exp_name
